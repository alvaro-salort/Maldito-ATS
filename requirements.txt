# Dependencias de Maldito ATS
# Instalación: pip install -r requirements.txt

# Extracción de texto y validación de PDFs para ATS
pypdf>=3.0.0

# Scraping web ético y consumo de APIs de empleo
requests>=2.28.0
beautifulsoup4>=4.11.0

# Conversión y procesamiento de datasets salariales (Excel / CSV)
openpyxl>=3.0.0

# Suite de pruebas automatizadas (opcional para desarrollo)
pytest>=7.0.0
